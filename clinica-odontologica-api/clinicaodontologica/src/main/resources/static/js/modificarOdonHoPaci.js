// modificar.js
function modificarPaciente(id) {
    // Puedes implementar la lógica para redireccionar a la página de modificación o abrir un formulario de modificación
    // Asegúrate de obtener la información del paciente por su ID antes de mostrar el formulario.
    console.log(`Modificar paciente con ID: ${id}`);
}

function modificarOdontologo(id) {
    // Puedes implementar la lógica para redireccionar a la página de modificación o abrir un formulario de modificación
    // Asegúrate de obtener la información del odontólogo por su ID antes de mostrar el formulario.
    console.log(`Modificar odontólogo con ID: ${id}`);
}